{
  "name": "app-builder-v2",
  "version": "0.1.0",
  "description": "PRD-driven full-stack app generator CLI",
  "license": "MIT",
  "type": "module",
  "bin": {
    "app-builder": "./dist/src/index.js"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test tests/analyze-metrics.test.mjs && node dist/tests/env.test.js && node dist/tests/parser.test.js && node dist/tests/generator.test.js && node dist/tests/cli.test.js",
    "lint": "node ./scripts/lint.mjs",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@langchain/anthropic": "^1.3.28",
    "@langchain/google": "^0.1.12",
    "@langchain/openai": "^1.4.5",
    "deepagents": "^1.10.0",
    "ink": "^7.0.2",
    "langchain": "^1.4.0",
    "playwright": "^1.60.0",
    "react": "^19.2.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "typescript": "^6.0.3"
  }
}
